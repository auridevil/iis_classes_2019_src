{
  "name": "iis_classes_2019",
  "version": "0.0.1",
  "author": "Aureliano Bergese <aureliano.bergese@gmail.com>",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "rimraf dist && yarn -s og-image && yarn -s deck",
    "deck": "marp --no-stdin PITCHME.md -o dist/index.html && cpx \"assets/**/*\" dist/assets",
    "now-build": "yarn -s deck",
    "og-image": "marp PITCHME.md -o dist/og-image.jpg",
    "start": "marp -ps ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^0.14.1",
    "cpx": "^1.5.0",
    "rimraf": "^3.0.0"
  }
}
